# =============================================================================
# Hecatrace — Python dependencies
# =============================================================================
# Note: plaso is installed separately BEFORE this file in the Dockerfile
# to avoid dependency resolution conflicts.
# =============================================================================

# --- Hecatrace orchestrator ---
PyYAML>=6.0
rich>=13.0

# --- Memory forensics ---
volatility3>=2.4.0

# --- Filesystem / image forensics ---
# pytsk3 and libewf-python require libtsk-dev and libewf-dev (installed via apt)
# libewf-python is the PyPI distribution name for the 'pyewf' module
pytsk3
libewf-python
dissect
dissect.target

# --- Malware analysis ---
yara-python>=4.3.1
pefile>=2023.2.7
pycryptodome>=3.18.0
python-magic>=0.4.27

# --- Document / Office analysis ---
oletools>=0.60

# --- Hashing ---
ssdeep
tlsh-python
